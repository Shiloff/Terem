@model ProfileActionsResult
@foreach (var action in Model.Actions)
{
    
        @Html.Partial("LoadAction", new AddActionResult
       {
           Profile = Model.Profile,
           ProfileAction = action,
           ProfileActionComments = action.ProfileActionComments,
       })
}
